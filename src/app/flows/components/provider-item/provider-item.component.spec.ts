/* tslint:disable: ter-max-len */
describe('Flows App', () => {
  describe('ProviderDetail Component', () => {

    // UI
    // - should display item as selected if is selected
    // - I can select the step

    describe('select()', () => {
      xit('should emit an onSelectProvider event with the correct params', () => {
      });
    });
  });
});
